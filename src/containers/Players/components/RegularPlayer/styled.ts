import styled from "styled-components";

const StyledRegularPlayer = styled.div`
  display: flex;
  width: 20rem;
  justify-content: space-between;
  margin-bottom: 1rem;
`;

export default StyledRegularPlayer;
