import styled from "styled-components";

const StyledFavoritePlayer = styled.div`
  display: flex;
  width: 20rem;
  justify-content: space-between;
  padding-bottom: 1rem;
`;

export default StyledFavoritePlayer;
